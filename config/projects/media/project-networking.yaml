apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: media-project
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  description: Project for networking related applications
  destinations:
  - namespace: jellyfin
    server: https://kubernetes.default.svc
  sourceRepos:
    - '*'
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'