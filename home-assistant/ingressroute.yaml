---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: homeassistant
  namespace: homeassistant
  labels:
    app.kubernetes.io/name: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`homeassistant.monroe.gg`)
      priority: 10
      services:
        - kind: Service
          name: homeassistant
          namespace: homeassistant
          port: 8123
  tls:
    certResolver: porkbun